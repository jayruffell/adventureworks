*ctrl + k, v to view a preview of this file in markdown in VS Code, with my markdown extension*

# overview
just quickly testing out SSRS for a job interview.

I used Report Server Configuration Manager  to set up the SSRS  webpage (and looking at the RSCM "web page" tab will show what the webpage is that hosts the report) and to connect to SQLDEV  server instance.

Then I used Visual Studio, using a Reporting Services  project template (or similar, as suggested by ChatGPT)  to design and deploy the report.

The report lives here http://desktop-jgnu8d2/Reports/browse/ -  although I'm not sure if it is hosted there always or if I need to reopen Visual Studio and/or the SQLDEV instance and redeploy in order for it to show up again.

